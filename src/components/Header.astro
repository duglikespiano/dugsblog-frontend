---
import { routes } from '../common/variables';
const { language } = Astro.params;
---

<header class="flex justify-between">
	<h1 class="text-rosewater font-extrabold text-6xl">
		<a href="/" class="no-underline align-text-top"> Dug's Blog</a>
	</h1>

	<nav class="flex items-center">
		<ul class="flex text-4xl gap-4 text-rosewater font-bold">
			{
				routes.map((route) => (
					<li class="transition-text hover:text-gray3 duration-300">
						<h2>
							<a class="no-underline" href={`/${language}/${route.url}`}>
								{route.name}
							</a>
						</h2>
					</li>
				))
			}
		</ul>
	</nav>
</header>
